#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);  // Number of test cases

    while (T--) {
        int N;
        scanf("%d", &N);  // Number of women

        int count = 0;    // Counter for eligible women
        for (int i = 0; i < N; i++) {
            int age;
            scanf("%d", &age);  // Read each woman's age
            if (age >= 10 && age <= 60) {
                count++;  // Increment if age is eligible
            }
        }

        printf("%d\n", count);  // Output eligible count for this test case
    }

    return 0;
}
