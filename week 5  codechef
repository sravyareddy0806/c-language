#include <stdio.h>

int main() 
{
    int N
    if (scanf("%d", &N) == 1) .
{
        if (N >= 6 && N <= 8)
{
            printf("Yes\n");
        } else{
            printf("No\n");
        }
    }

    return 0;
}
